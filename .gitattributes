# No Git LFS in this repo. Large generated binaries (*.mbtiles, *.wasm outside
# docs/, *.glb, *.a, build trees, node_modules/) are git-ignored (see
# .gitignore) and rebuilt locally instead of being committed or stored in LFS.
# docs/**/*.wasm stays tracked as plain blobs for GitHub Pages.
