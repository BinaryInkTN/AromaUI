# Git LFS only for blobs that exceed GitHub's 100MB plain-file limit.
# Built .apak packages are git-ignored (see .gitignore): every build packs
# its own matching-arch copies, so they are never committed.
examples/package_examples/nav/assets/ariana_sat.mbtiles filter=lfs diff=lfs merge=lfs -text
