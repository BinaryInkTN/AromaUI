# Git LFS only for the two blobs that exceed GitHub 100MB plain-file limit.
# Everything else is stored as plain blobs.
examples/car_infotainment/assets/com.aroma.nav.apak filter=lfs diff=lfs merge=lfs -text
examples/package_examples/nav/assets/ariana_sat.mbtiles filter=lfs diff=lfs merge=lfs -text
